# InventoryAPI

This repository contains the InventoryAPI for Utensils Management System.

Run locally:
- Configure environment variables:
  - JWT_KEY
  - ConnectionStrings__DefaultConnection
- dotnet run --project InventoryAPI

Docker:
- docker-compose up --build
