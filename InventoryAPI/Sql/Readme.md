This folder holds SQL files. They are embedded as resources and can be loaded via SqlResourceLoader.GetSql("Folder.File.sql").

Naming convention: use folder and filename to form resource key. Example: "Sql.Categories.GetById.sql"
