apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventoryapi.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "inventoryapi.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
